% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/H2.fast.R
\name{H2.fast}
\alias{H2.fast}
\title{Optimized specialization index H2' of DOM-microbe associations}
\usage{
H2.fast(
  mol.data,
  micro.data,
  occu.rate = 0.3,
  sparcc.R = NULL,
  sparcc.R.threshold = 0.3,
  N = 100,
  Null.model = "shuffle.web",
  network.type = c("Full", "Positive", "Negative"),
  ncores = 3,
  verbose = interactive()
)
}
\arguments{
\item{mol.data}{Sample/DOM matrix with samples in rows and DOM molecules in columns (compositional or abundance data).}

\item{micro.data}{Sample/microbe matrix with samples in rows and microbial taxa in columns (compositional or abundance data).}

\item{occu.rate}{The threshold for retaining bacterial taxa or DOM molecules observed in a specified proportion of the total samples. Default: 0.3.}

\item{sparcc.R}{An optional precomputed SparCC correlation matrix. If \code{NULL}, it will be computed internally. Default: \code{NULL}.}

\item{sparcc.R.threshold}{The absolute SparCC threshold used to retain DOM-microbe correlations in the bipartite network. Default: 0.3.}

\item{N}{Number of null models to generate. Default: 100.}

\item{Null.model}{Null model type. Can be given as an integer or name: 1/"r2dtable", 2/"swap.web", 3/"vaznull", 4/"shuffle.web". Default: \code{"shuffle.web"}.}

\item{network.type}{Character vector giving the network types to analyse. Available options are \code{"Full"}, \code{"Positive"}, and \code{"Negative"}. Default is all three.}

\item{ncores}{Number of CPU cores used for sample-level parallel computing. Default is 3, which uses serial execution.}

\item{verbose}{Logical; if \code{TRUE}, emits status messages. Default: \code{interactive()}. When enabled, \code{H2.fast()} reports preprocessing and SparCC status messages together with network-analysis progress updates.}
}
\value{
A data frame containing \code{Index}, \code{Observed}, \code{Standardised}, \code{p.value}, \code{Network.type}, and \code{Sample}.
}
\description{
This function calculates the network-level specialization index H2' for full, negative, and positive DOM-microbe bipartite networks.
This optimized version keeps the original \code{H2()} function unchanged and adds optional sample-level parallel execution.
}
\details{
The goal of \code{H2.fast()} is to accelerate the existing workflow without changing the core H2' calculation, null-model strategy, or output structure used by \code{H2()}.
If \code{sparcc.R} is not supplied, the suggested package \code{SpiecEasi} is required.
When \code{ncores > 1}, sample-level calculations are distributed with a cross-platform PSOCK cluster via \code{parallel::parLapply()}.
}
\examples{
\donttest{
data(mol.data)
data(micro.data)

mol_ex <- mol.data[, 1:60]
mic_ex <- micro.data[, 1:60]

norm.mol <- vegan::decostand(mol_ex, method = "total")
norm.mic <- vegan::decostand(mic_ex, method = "total")
mm <- cbind(
  as.matrix(round(100000 * norm.mol, 0)),
  as.matrix(round(100000 * norm.mic, 0))
)
keep <- colSums(mm > 0) >= nrow(mm) * 0.3
mm_keep <- mm[, keep, drop = FALSE]
if (requireNamespace("SpiecEasi", quietly = TRUE)) {
  sparcc_ex <- SpiecEasi::sparcc(mm_keep)[["Cor"]]
  colnames(sparcc_ex) <- rownames(sparcc_ex) <- colnames(mm_keep)
} else {
  sparcc_ex <- stats::cor(mm_keep)
}

res <- H2.fast(
  mol.data = mol_ex,
  micro.data = mic_ex,
  sparcc.R = sparcc_ex,
  N = 5,
  ncores = 1,
  network.type = "Full",
  verbose = FALSE
)
head(res)
}
}
\references{
A Hu, M Choi, A J Tanentzap, J Liu, K-S Jang, J T Lennon, Y Liu, J Soininen, X Lu, Y Zhang, J Shen, and J Wang. 2022.
Ecological networks of dissolved organic matter and microorganisms under global change.
\emph{Nature Communications}. \url{https://www.nature.com/articles/s41467-022-31251-1}

F Meng, A Hu, K-S Jang, and J Wang. 2025.
iDOM: Statistical analysis of dissolved organic matter characterized by high-resolution mass spectrometry.
\emph{mLife}. \url{https://onlinelibrary.wiley.com/doi/10.1002/mlf2.70002}
}
\seealso{
\code{\link{H2}}, \code{\link[vegan]{decostand}},
\code{\link[bipartite]{nullmodel}}, \code{\link[bipartite]{networklevel}}
}
